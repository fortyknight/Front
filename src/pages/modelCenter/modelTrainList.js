import React from 'react';
import { Input } from 'antd';
import { withRouter } from 'umi';

const ModelTrainList = () => <Input placeholder="Basic usage" />;
export default withRouter(ModelTrainList);